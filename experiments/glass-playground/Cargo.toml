[package]
name = "doubao-glass-playground"
version = "0.1.0"
edition = "2021"
rust-version = "1.82"
publish = false

[dependencies]
doubao-glass-live = { path = "../glass-live", features = ["playground"] }
image = { version = "=0.25.5", default-features = false, features = ["png", "webp"] }
